// TODO: Fix the function body without changing the signature.
fn square(num: i32) -> i32 {
    return num * num;
}

fn main() {
    let answer = square(90);
    println!("The square of 90 is {answer}");
}
